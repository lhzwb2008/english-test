# Qwen 生产机 SSH（复制为仓库根目录 .deploy.env 后填写）
# .deploy.env 已在 .gitignore，不会进 Git。
#
# 密码含 $、!、空格等特殊字符时，请用单引号包住，例如：
#   DEPLOY_PASSWORD='p@ss word$'

DEPLOY_HOST=101.201.237.149
DEPLOY_USER=root
DEPLOY_PASSWORD=

# 可选
DEPLOY_SSH_PORT=22
DEPLOY_PATH=/opt/qwen-oral-proxy
